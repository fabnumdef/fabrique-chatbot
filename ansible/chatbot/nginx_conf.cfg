server {
        listen 80 default_server;
        listen [::]:80 default_server;
        root /var/www/chatbot-front;
        location / {
          try_files $uri $uri/ /index.html;
        }
        location /api/ {
          proxy_pass http://127.0.0.1:3000/api/;
        }
}
